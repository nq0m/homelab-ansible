#!/bin/bash

/usr/bin/cp /srv/certs/.lego/certificates/mediatools.servers.test-net.work.pem /srv/certs/
/usr/bin/chown 99.99 /srv/certs/mediatools.servers.test-net.work.pem

# Restart haproxy container to put new certs live

/usr/bin/podman container kill --signal HUP mediatools_haproxy_1