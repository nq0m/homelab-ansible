#!/bin/bash

/usr/bin/cp /srv/certs/.lego/certificates/docker.servers.test-net.work.pem /srv/certs/
/usr/bin/chown 99.99 /srv/certs/docker.servers.test-net.work.pem

# Restart haproxy container to put new certs live

/usr/bin/podman --signal HUP tools_haproxy_1