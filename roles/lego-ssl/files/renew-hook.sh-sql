#!/bin/bash

/usr/bin/cp /srv/certs/.lego/certificates/sql.servers.test-net.work.pem /srv/certs/
/usr/bin/chown 99.99 /srv/certs/sql.servers.test-net.work.pem

# Restart haproxy container to put new certs live

/usr/bin/podman container kill --signal HUP sql_haproxy_1