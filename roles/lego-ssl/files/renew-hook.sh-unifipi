#!/bin/bash

/usr/bin/cp /srv/certs/.lego/certificates/unifipi.network.test-net.work.pem /srv/certs/
/usr/bin/chown 99.99 /srv/certs/unifipi.network.test-net.work.pem

# Restart haproxy container to put new certs live

/usr/bin/podman container kill --signal HUP unifi_haproxy_1