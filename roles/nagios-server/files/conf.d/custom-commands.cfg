define command{
        command_name    check_ping
        command_line    $USER1$/check_ping $ARG1$
        }

define command {
    command_name    check_ssh
    command_line    $USER1$/check_ssh $ARG1$
}

define command {
    command_name    check-host-alive
    command_line    $USER1$/check_ping -H $HOSTADDRESS$ -w 3000.0,80% -c 5000.0,100% -p 5
}

define command{
        command_name    notify-service-by-slack
        command_line    /usr/local/bin/slack_nagios.pl -field slack_channel=#nagios -field HOSTALIAS="$HOSTNAME$" -field SERVICEDESC="$SERVICEDESC$" -field SERVICESTATE="$SERVICESTATE$" -field SERVICEOUTPUT="$SERVICEOUTPUT$" -field NOTIFICATIONTYPE="$NOTIFICATIONTYPE$"
}

define command{
        command_name    notify-host-by-slack
        command_line    /usr/local/bin/slack_nagios.pl -field slack_channel=#nagios -field HOSTALIAS="$HOSTNAME$" -field HOSTSTATE="$HOSTSTATE$" -field HOSTOUTPUT="$HOSTOUTPUT$" -field NOTIFICATIONTYPE="$NOTIFICATIONTYPE$"
}

define command{
    command_name notify-by-pushover-service
    command_line /usr/bin/printf "%b" "$NOTIFICATIONTYPE$: $SERVICEDESC$@$HOSTNAME$: $SERVICESTATE$ ($SERVICEOUTPUT$)" | /usr/local/bin/notify-by-pushover.php SERVICE ao56mo73eow2aj9pxmecnib79dgvgp $CONTACTADDRESS1$ $NOTIFICATIONTYPE$ $SERVICESTATE$
}

define command{
  command_name notify-by-pushover-host
  command_line /usr/bin/printf "%b" "Host '$HOSTALIAS$' is $HOSTSTATE$: $HOSTOUTPUT$" | /usr/local/bin/notify-by-pushover.php HOST ao56mo73eow2aj9pxmecnib79dgvgp $CONTACTADDRESS1$ $NOTIFICATIONTYPE$ $HOSTSTATE$
}