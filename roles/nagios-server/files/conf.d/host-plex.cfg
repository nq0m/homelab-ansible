define host{
        use                     test-net-host
        host_name               plex.wireless.test-net.work
        alias                   plex.wireless.test-net.work
        address                 plex.wireless.test-net.work
}

define service{
        use                     test-net-service
        host_name               plex.wireless.test-net.work
        service_description     PING
        check_command           check_ping!-H plex.wireless.test-net.work -w 100.0,20% -c 500.0,60% -p 5
}

define service{
        use                             test-net-service
        host_name                       plex.wireless.test-net.work
        service_description             SSH
        check_command                   check_ssh!-H plex.wireless.test-net.work
        }

define service{
        use                             test-net-service
        host_name                       plex.wireless.test-net.work
        service_description             Plex Server
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_procs --ereg-argument-array='Plex Media Server' -w 1:1 -c 1:1"!nagcheck
        }

define service{
        use                             test-net-service
        host_name                       plex.wireless.test-net.work
        service_description             HTTPS - Portainer
        check_command                   check_http!--ssl --sni -C 14 -I plex.wireless.test-net.work -H plex.wireless.test-net.work -p 9000 -u /
        }

define service{
        use                             test-net-service
        host_name                       plex.wireless.test-net.work
        service_description             Disk Space /
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_disk -w 10% -c 5% -p /"!nagcheck
        }

define service{
        use                             test-net-service
        host_name                       plex.wireless.test-net.work
        service_description             Chrony
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_chrony.sh 500 1000"!nagcheck
        }