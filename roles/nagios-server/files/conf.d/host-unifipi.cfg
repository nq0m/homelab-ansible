define host{
        use                     test-net-host
        host_name               unifipi.network.test-net.work
        alias                   unifipi.network.test-net.work
        address                 unifipi.network.test-net.work
}

define service{
        use                     test-net-service
        host_name               unifipi.network.test-net.work
        service_description     PING
        check_command           check_ping!-H unifipi.network.test-net.work -w 100.0,20% -c 500.0,60% -p 5
}

define service{
        use                             test-net-service
        host_name                       unifipi.network.test-net.work
        service_description             SSH
        check_command                   check_ssh!-H unifipi.network.test-net.work
        }

define service{
        use                             test-net-service
        host_name                       unifipi.network.test-net.work
        service_description             HTTPS - Unifi Controller
        check_command                   check_http!--ssl --sni -C 14 -I unifipi.network.test-net.work -H unifi-controller.network.test-net.work -u /
        }

define service{
        use                             test-net-service
        host_name                       unifipi.network.test-net.work
        service_description             HTTPS - Portainer
        check_command                   check_http!--ssl --sni -C 14 -I unifipi.network.test-net.work -H unifipi.network.test-net.work -p 9000 -u /
        }

define service{
        use                             test-net-service
        host_name                       unifipi.network.test-net.work
        service_description             Disk Space /
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_disk -w 10% -c 5% -p /"!nagcheck
        }

define service{
        use                             test-net-service
        host_name                       unifipi.network.test-net.work
        service_description             Chrony
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_chrony.sh 500 1000"!nagcheck
        }