define host{
        use                     test-net-host
        host_name               backuppi.servers.test-net.work
        alias                   backuppi.servers.test-net.work
        address                 backuppi.servers.test-net.work
}

define service{
        use                     test-net-service
        host_name               backuppi.servers.test-net.work
        service_description     PING
        check_command           check_ping!-H backuppi.servers.test-net.work -w 100.0,20% -c 500.0,60% -p 5
}

define service{
        use                             test-net-service
        host_name                       backuppi.servers.test-net.work
        service_description             SSH
        check_command                   check_ssh!-H backuppi.servers.test-net.work
        }

define service{
        use                             test-net-service
        host_name                       backuppi.servers.test-net.work
        service_description             Disk Space /
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_disk -w 10% -c 5% -p /"!nagcheck
        }

define service{
        use                             test-net-service
        host_name                       backuppi.servers.test-net.work
        service_description             Chrony
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_chrony.sh 500 1000"!nagcheck
        }