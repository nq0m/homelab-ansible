define host{
        use                     test-net-host
        host_name               docker.servers.test-net.work
        alias                   docker.servers.test-net.work
        address                 docker.servers.test-net.work
}

define service{
        use                     test-net-service
        host_name               docker.servers.test-net.work
        service_description     PING
        check_command           check_ping!-H docker.servers.test-net.work -w 100.0,20% -c 500.0,60% -p 5
}

define service{
        use                             test-net-service
        host_name                       docker.servers.test-net.work
        service_description             SSH
        check_command                   check_ssh!-H docker.servers.test-net.work
        }

define service{
        use                             test-net-service
        host_name                       docker.servers.test-net.work
        service_description             HTTPS - Portal
        check_command                   check_http!--ssl --sni -C 14 -I portal.servers.test-net.work -H portal.servers.test-net.work -u /
        }

define service{
        use                             test-net-service
        host_name                       docker.servers.test-net.work
        service_description             HTTPS - Grafana Dashboard
        check_command                   check_http!--ssl --sni -C 14 -I dashboard.servers.test-net.work -H dashboard.servers.test-net.work -u /
        }

define service{
        use                             test-net-service
        host_name                       docker.servers.test-net.work
        service_description             HTTPS - Portainer
        check_command                   check_http!--ssl --sni -C 14 -I docker.servers.test-net.work -H docker.servers.test-net.work -p 9000 -u /
        }

define service{
        use                             test-net-service
        host_name                       docker.servers.test-net.work
        service_description             Disk Space /
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_disk -w 10% -c 5% -p /"!nagcheck
        }

define service{
        use                             test-net-service
        host_name                       docker.servers.test-net.work
        service_description             Chrony
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_chrony.sh 500 1000"!nagcheck
        }

define service{
        use                             test-net-service
        host_name                       docker.servers.test-net.work
        service_description             InfluxDB
        check_command                   check_tcp! -H $HOSTADDRESS$ -p 8080
        }