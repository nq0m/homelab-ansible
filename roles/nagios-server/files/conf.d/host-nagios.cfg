define host{
        use                     test-net-host
        host_name               nagios.servers.test-net.work
        alias                   nagios.servers.test-net.work
        address                 nagios.servers.test-net.work
}

define service{
        use                     test-net-service
        host_name               nagios.servers.test-net.work
        service_description     PING
        check_command           check_ping!-H nagios.servers.test-net.work -w 100.0,20% -c 500.0,60% -p 5
}

define service{
        use                             test-net-service
        host_name                       nagios.servers.test-net.work
        service_description             SSH
        check_command                   check_ssh!-H nagios.servers.test-net.work
        }

define service{
        use                             test-net-service
        host_name                       nagios.servers.test-net.work
        service_description             HTTPS
        check_command                   check_http!--ssl --sni -C 14 -I nagios.servers.test-net.work -H nagios.servers.test-net.work -u /nagios.txt
        }

define service{
        use                             test-net-service
        host_name                       nagios.servers.test-net.work
        service_description             Disk Space /
        check_command                   check_by_ssh! -C "/usr/lib64/nagios/plugins/check_disk -w 10% -c 5% -p /"!nagcheck
        }

define service{
        use                             test-net-service
        host_name                       nagios01.servers.test-net.work
        service_description             Chrony
        check_command                   check_by_ssh! -C "/usr/lib/nagios/plugins/check_chrony.sh 500 1000"!nagcheck
        }